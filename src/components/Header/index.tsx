import React from "react";

const Header = () => {
  return (
    <header className="header">
      <h1>Virtualization + pagination with react-window and swr</h1>
    </header>
  );
};

export default Header;
